You are my AI trends research assistant. Search the web for the most current
information and gather content for a dashboard covering the last 30 days.
Use today's date as the anchor and only include items published within that
window.

Cover these three sections:

1. LATEST AI NEWS
   The most significant developments, model releases, funding rounds,
   research breakthroughs, and industry moves. For each item give a title,
   a one-line summary, the source name, the publish date, and a direct link
   to the original article (not a search results page or aggregator listing).

2. TRENDING AI AGENTS
   AI agents, agent frameworks, and autonomous tools that are gaining
   traction right now. For each, note what it does, why it is getting
   attention right now, and a direct link to its homepage, repo, or the
   article you found it in.

3. TRENDING AI GITHUB REPOS
   Repositories in the AI and agent space that are rising fast. For each,
   give the repo name, a short description, approximate star count or star
   growth, primary language, and a direct link to the repo on GitHub.

Requirements:
- Prioritize the newest and most credible sources over aggregators.
- Rank items within each section by relevance and momentum.
- Every link must be a real, working URL you found via search — never a
  placeholder, a search-engine query URL, or a guess.
- Aim for 6-8 items in LATEST AI NEWS, 4-6 in TRENDING AI AGENTS, and 6-8 in
  TRENDING AI GITHUB REPOS.
- Write plain text only in every field — no markdown syntax (no `[text](url)`
  links, no `**bold**`, no bullet characters). Each field is rendered as-is,
  so markdown would show up as literal text instead of formatting.
- Also write 3-5 short "top takeaways" bullet points summarizing the window
  as plain text sentences.
